---
title: 今天
date: 2026-08-05 13:10:20
---
今天到我的高中去取录取通知书

就看到一位初中同学

我们现在分的是虚拟班级，之后还会再分班

发了一本作业，一个通知书，一份传单，一个布袋，还是挺好的

不对不对！我仔细看了一下这个作业，
这个升学e网通什么鬼？！不仅要看网课，还要交作业。。。

顿时感觉很无语

<style>
  .e2147-badge {
    /* ===== 主题变量，方便换肤 ===== */
    --badge-fg: #000;
    --badge-bg: #fff;
    --badge-radius: 6px;
    --badge-shadow: 3px 3px 0px var(--badge-fg);

    display: inline-flex;
    font-family: 'Courier New', Courier, monospace;
    font-size: 14px;
    font-weight: bold;
    border-radius: var(--badge-radius);
    overflow: hidden;
    border: 2px solid var(--badge-fg);
    vertical-align: middle;
    line-height: 1;
    text-decoration: none;
    box-shadow: var(--badge-shadow);
    cursor: default;
    user-select: none;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }

  /* ===== 悬浮微交互：轻微上浮 + 阴影加深 ===== */
  .e2147-badge:hover {
    transform: translate(-1px, -1px);
    box-shadow: 4px 4px 0px var(--badge-fg);
  }
  .e2147-badge:active {
    transform: translate(1px, 1px);
    box-shadow: 1px 1px 0px var(--badge-fg);
  }

  /* ===== 左右分区 ===== */
  .e2147-badge .badge-left {
    background: var(--badge-fg);
    color: var(--badge-bg);
    padding: 6px 10px;
  }
  .e2147-badge .badge-right {
    background: var(--badge-bg);
    color: var(--badge-fg);
    padding: 6px 10px;
    display: inline-flex;
    align-items: center;
    gap: 2px;
  }

  /* ===== 闪烁光标（dev 氛围感） ===== */
  .e2147-badge .badge-right::after {
    content: '▌';
    font-size: 12px;
    animation: blink 1s step-end infinite;
    opacity: 0.7;
  }
  @keyframes blink {
    0%, 100% { opacity: 0.7; }
    50%      { opacity: 0; }
  }
</style>

<div class="e2147-badge" role="img" aria-label="e2147 dev badge">
  <span class="badge-left">e2147</span>
  <span class="badge-right">dev</span>
</div>